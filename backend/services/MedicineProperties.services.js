// takes the medical properties and sends them into another URL fetch to get properties.  
const { externalAPI } = require('./RxNormAPI.services');  


const MedicineProperties = async (MedicalData) => {     
    // start of with base cases and error handling then move onto try and catch case here as such 
    
    try { 

    } catch (error) { 
        
    }

}; 



module.exports = MedicineProperties; 